#!/bin/bash

set -e

INKSCAPE="/Applications/Inkscape.app/Contents/MacOS/inkscape"

height="$("$INKSCAPE" -w $3 -o `pwd`/"$2" `pwd`/"$1" 2>&1 | awk '/exported to/ {print $7}')"

#echo "Height: $height"

if [ $height -gt $3 ] ; then
    "$INKSCAPE" -h $3 -o `pwd`/"$2" `pwd`/"$1" > /dev/null
fi
